import 'package:flutter/material.dart';

class TestNextPage extends StatelessWidget {
  const TestNextPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is a test page.'),
    );
  }
}